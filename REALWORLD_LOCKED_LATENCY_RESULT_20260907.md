# 真机双机链路静止延迟实测（2026-09-07）

## 结论

新分支已完成真实 Jetson 相机 → RTX 模型 → Jetson 适配器的静止实测。
稳态 CEC 规划的 RGB 时间戳到响应返回：中位数 **0.765 s**、P95 **0.894 s**，
实际约 **1 Hz**；单独几何更新窗口达到 **2.9 Hz**。
本次没有驱动机器人，因此不能据此宣布运动跟踪、末端接近或自动停车已经修复。

另两个重要边界：

- 全部 137 次规划均由 CEC 接管，选中历史 anchor 23；这不是只测便宜的 native fallback。
  但它也是单目标、静止视角、缓存命中的工作负载，不代表切换 anchor 或新场景的最坏延迟。
- 近目标配置已启用，但 137 次直接目标证据均不足，未触发接近/朝向分支。
  本次不能关闭 pair_006 的末端动作问题，也不能代替一轮导航成功率实验。

## 1. 测量对象与隔离

| 项目 | 本次配置 |
|---|---|
| 代码 | `feat/sim-aligned-receding-horizon-20260907`，`e45b302907afaea9f0bb13b6167bd890320fa783` |
| Jetson | Orin NX，当前 15 W / 四 CPU 核；未切换功耗模式 |
| 相机 | 实时 D435i，848 × 480 @ 30 Hz，USB 3.2 |
| GPU | RTX 4090，驱动报告总显存 49,140 MiB |
| 方法 | Full-mono CEC；`terminal_approach=height_scaled_local` |
| 策略输入 | LingBot 单目深度；相机高度先验 0.42 m；D435i 深度不进入策略 |
| 历史 | 既有封存真实 Survey 的隔离副本，153 帧 |
| 目标 | Episode 20260906T100849_093505Z 的固定外部 Revisit 图 |
| 调度配置 | 规划 1.5 Hz，几何 3 Hz，控制回调 20 Hz |
| 运动状态 | `enabled=false`、`estop=true`，只发布到隔离的 `/cec_latency/cmd_vel` |
| 现场限制 | 相机和 Jetson 在线；Go2 地址 192.168.123.161 未响应，未启动电机桥 |

GPU 上使用独立端口 28888/28889/28890、独立缓冲区与历史副本。
没有 reset 原有 18888/8888 服务，没有切换原 GPU 或 Jetson 工作区版本，
没有改写实验 pair registry，也没有重新采集/覆盖 Survey。

NavDP FIFO 只由当前 query-start 一帧初始化；153 帧历史没有重放进 NavDP FIFO。
实验工具是 [measure_locked_latency.py](deployment/go2/measure_locked_latency.py)，
使用真实适配器、真实模型和实时相机，不是模型 mock 或单元测试。

## 2. 时序与统计口径

共 155 s：15 s warmup → 30 s 普通规划 → 30 s 加到达匹配负载 →
20 s 仅几何更新 → 30 s 加匹配负载 → 30 s 普通规划。

共收到 **207** 个 HTTP 响应：

- 137 次策略规划，其中 warmup 14 次，稳态 **123 次**。
- 70 次 observation-only，其中专用 20 s 窗口 58 次。
- 207/207 HTTP 200，传输异常 0。
- 3,094 次禁用状态控制回调全部保持急停和零速度，非零命令尝试 0。

时延定义：

- **HTTP 耗时**：Jetson HTTP send 开始至完整响应体接收；包含 SSH 隧道往返和 GPU 服务处理，
  不包含 send 之前的客户端图像序列化。
- **RGB → 响应**：用同一 Jetson 时钟计算，从源 RGB 时间戳到响应体收到；
  包括相机帧年龄、客户端处理与请求。这不是纯网络延迟，也还不是电机执行延迟。
- **控制周期**：禁用控制回调的开始时间间隔；不能解释成移动轨迹跟踪计算的最坏耗时。
- 原始 `summary.json` 按阶段汇总，部分阶段混有几何请求。
  下表按 endpoint 重新分组，避免用更快的几何请求拉低策略规划时延。

## 3. 核心结果

| 测量 | N | 中位数 | P95 | 最大值 |
|---|---:|---:|---:|---:|
| 稳态规划 HTTP | 123 | 0.602 s | 0.617 s | 0.632 s |
| 稳态规划 RGB → 响应 | 123 | 0.765 s | 0.894 s | 1.009 s |
| 无到达匹配时，规划 RGB → 响应 | 60 | 0.742 s | 0.867 s | 0.905 s |
| 加到达匹配时，规划 RGB → 响应 | 63 | 0.774 s | 0.912 s | 1.009 s |
| 仅几何窗口 HTTP | 58 | 0.250 s | 0.257 s | 0.313 s |
| 仅几何窗口 RGB → 响应 | 58 | 0.356 s | 0.462 s | 0.505 s |
| Jetson 到达匹配计算 | 562 | 80.4 ms | 106.4 ms | 129.5 ms |
| 无匹配负载的控制回调间隔 | 1,198 | 50.0 ms | 54.5 ms | 61.4 ms |
| 有匹配负载的控制回调间隔 | 1,198 | 50.0 ms | 57.8 ms | 79.8 ms |

两个普通规划窗口合计 60 次/60 s；两个匹配负载窗口合计 63 次/60 s。
所以实际是 **约 1 Hz，而不是配置值 1.5 Hz**。
几何专用窗口 58 次/20 s = **2.9 Hz**。

稳态 probe 收据中，几何 append 中位数 265.3 ms，
retrieval 中位数 178.8 ms，probe total 中位数 444.7 ms。
缓存 relocalization 中位数 0.297 ms；首次则为约 1.81 s。
本次响应没有独立 NavDP runtime 字段，因此不把总耗时差额冒充 diffusion-only 时间。

## 4. 冷启动与历史准备

| 阶段 | 耗时 |
|---|---:|
| 隔离服务 episode reset | 1.116 s |
| 153 帧 Survey 加载/重放 | 30.083 s |
| 安装目标与 query-start 初始化 | 0.129 s |
| warmup 最慢一次规划 HTTP | 2.602 s |
| warmup 最慢一次 RGB → 响应 | 2.811 s |

30.083 s 是一次性的历史准备，不是每次重规划耗时；也不包含所有模型进程的权重加载时间。
first40 尺度收据中的约 4.50 s 是历史准备的尺度估计记录，不是每次策略请求重复执行。

warmup 的一个 2.811 s 响应触发 `plan_input_too_old_hold`，
被记录为 `late_plan_discarded`，没有当作新鲜轨迹接纳。
其余 136 个规划产生正常计划收据；稳态没有超过 1.5 s 的响应。
没有通过放宽时效门槛隐藏这个冷启动尾延迟。

首次诊断脚本启动曾失败：从 JSON 导出 ROS YAML 时，3.0 被写成整数 3，
与 ROS double 参数声明冲突。修正诊断用 YAML 的数值类型后，
核验目标/历史身份并复用此次隔离测量自己的已准备状态，第二次完成。
这不是生产参数修改，也不是复用原正式实验服务。
首次失败日志仍保留，未将其算成成功试验。

## 5. 到达检测与几何更新实际验证了什么

到达负载使用现有 `RgbGoalArrivalVerifier` 的真实 SIFT/homography 计算，
目标宽度 480、SIFT 上限 1,200，使用默认判据。
它运行在隔离的 ROS timer 中，没有 arrival/STOP 发布权限；
没有测量独立 arrival daemon 的全部发布、日志、锁存与电机停止链路。

设定 12 Hz，实际 562 次/60 s = **9.37 Hz**。
P95 106 ms 已超过 12 Hz 的 83 ms 周期，说明当前 15 W Jetson 上
不能将这个匹配负载无条件称为稳定 12 Hz。
加负载后规划端到端 P95 从 0.867 s 变为 0.912 s，控制回调最坏间隔从
61.4 ms 变为 79.8 ms；本次没有控制回调长时间阻塞。
这些是短窗口实测，不是实时调度保证。

562 次匹配均未通过：226 次 good matches 不足、229 次 homography 失败、
107 次 inliers 不足。当前静止位置没有独立到达真值，
因此不能将它们记为 562 次到达漏检，也不能声称 STOP 已验证。

70 个几何更新收据均为 `policy_fifo_updated=false`、
`sealed_survey_updated=false`。207 次服务处理的 frame index
从 153 至 359 连续且唯一；不是重复计算同一缓存响应。
稳态 123 个 depth transaction ID 唯一，尺度收据不变，均未消费传感器 metric depth。

几何专用窗口是主动调用转身期间使用的同一 observation-only 路径，
没有实际转身或 IMU 角度变化。因此它证明计算与状态接口可持续更新，
不证明真实 160° 转身中的跟踪精度。
当前 IMU 没有输入 LingBot 位姿估计，连续纯旋转帧仍可能导致几何退化。
高度尺度校准不等于漂移校正；不能用本次 2.9 Hz 更新频率声称已经解决这个问题。

## 6. 对真机适配的判断与下一步

1. **静止情况下，通信/模型链路支持低速滚动重规划。**
   约 0.8 s 的反馈时延并不要求每次推理前先停住；新设计允许沿有效旧轨迹运动并后台获取新计划。
   但本次没有启用运动，尚未验证轨迹重投影和换轨后的实际平滑性。
2. **不能按“每 0.3 m 固定重规划”直接照抄仿真。**
   若速度为 0.3 m/s，0.765 s 约对应 0.23 m 的运动量，P95 对应约 0.27 m。
   这是延迟的量级换算，不是本次实测运动。必须结合 RGB 时刻位姿补偿和有效轨迹重叠执行。
3. **下一次应测试近目标动作链，而不是继续只测空转推理。**
   在机器人连接恢复且有运动授权后，验证正向低速更新、背向转身几何连续性、
   已知近目标位置的直接证据、半径收缩、朝向、独立视觉确认与 STOP。
   特别要检查 pair_006 的“近目标证据有了但控制仍按 2.5 m”是否真的消失。
4. **本轮不修改阈值/频率，也不宣称 SR 改善。**
   可后续单独比较较低到达匹配频率或独立进程的负载；本次证据不足以直接替换正式设置。

## 7. 证据位置与收尾

本机原始结果目录：

`runtime/latency_measure_20260907.cqhGz7/`

- `measurement_v2/live_trace.jsonl`：逐请求、控制与匹配记录。
- `measurement_v2/summary.json`：原阶段统计。
- `measurement_v2/endpoint_reanalysis.json`：本文使用的 endpoint 分组复算。
- `measurement_v2/preparation.json`：153 帧准备与目标绑定。
- `measurement_v2/final_hub_health.json`：停止临时服务前的状态。
- `measurement.log`、`measurement_v2.log`：首轮启动错误与完整成功轮日志。
- `resolved.json`、`jetson_params_v2.yaml`：本次实际配置。

Jetson 副本：`/home/unitree/.cache/cec-latency-20260907.ISgKz5/measurement_v2/`。

Survey manifest SHA-256：
`beed8632f77a0e2335bf3d9fe2e953cebc17f4f0b831e7767e9ba92bd8fc819a`。
测量后隔离副本与原 Survey 的 manifest 哈希一致，Hub 的封存历史仍为 153 帧。

三个临时 GPU 服务和专用 SSH 隧道均已停止；原 18888/8888 服务保留，
原 resident memory_frames=0、depth_transactions=0、ep_0006 均未改变。
GPU 占用恢复到 7,054 MiB、利用率 0%。未删除任何测量或实验数据。
生产工作区没有切换到本次诊断版本，机器人没有自动启用。
